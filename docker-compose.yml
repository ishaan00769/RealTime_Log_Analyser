version: '3.8'

services:
  # 1. The C++ Analytic Backend
  backend:
    build: ./backend
    ports:
      - "8080:8080"

  # 2. The React Frontend Dashboard
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend